// @ts-nocheck
"use client";
import React,{useEffect,useState}from'react';import LoginPage from '@/components/Login/LoginPage';import PostLogin from '@/components/PostLogin/PostLogin';import useTweaks from '@/components/Tweaks/useTweaks';import { SESSION_KEY,TWEAK_DEFAULTS } from './constants';import TweaksUI from './TweaksUI';import FabRoot from './FabRoot';import ManagerRoot from './ManagerRoot';import LabRoot from './LabRoot';
export default function AppRoot(){const[user,setUser]=useState(()=>{try{return JSON.parse(localStorage.getItem(SESSION_KEY)||'null')}catch{return null}});const[t,setTweak]=useTweaks(TWEAK_DEFAULTS);useEffect(()=>{const root=document.documentElement;root.style.setProperty('--tweak-signin-bg',t.signInBg);root.style.setProperty('--tweak-signin-fg',t.signInFg);root.style.setProperty('--tweak-fab-bg',t.fabBg)},[t.signInBg,t.signInFg,t.fabBg]);useEffect(()=>{if(user)localStorage.setItem(SESSION_KEY,JSON.stringify(user));else localStorage.removeItem(SESSION_KEY)},[user]);const tweaksUI=<TweaksUI t={t} setTweak={setTweak}/>;const onLogout=()=>setUser(null);if(!user)return <div data-screen-label="Login"><LoginPage onLogin={setUser}/>{tweaksUI}</div>;if(user.role==='fab_user')return <FabRoot user={user} onLogout={onLogout} tweaksUI={tweaksUI}/>;if(user.role==='lab_manager')return <ManagerRoot user={user} onLogout={onLogout} tweaksUI={tweaksUI}/>;if(user.role==='lab_mem'||user.role==='lab_member')return <LabRoot user={user} onLogout={onLogout} tweaksUI={tweaksUI}/>;return <div data-screen-label={`App · ${user.role}`}><PostLogin user={user} onLogout={onLogout}/>{tweaksUI}</div>}
